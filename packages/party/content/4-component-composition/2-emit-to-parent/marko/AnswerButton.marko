class {
  clickYes = function clickYes() {
    this.input.onYes();
  };
  clickNo = function clickNo() {
    this.input.onNo();
  };
}

<button on-click(event => component.clickYes(event))>YES</button>
<button on-click(event => component.clickNo(event))>NO</button>