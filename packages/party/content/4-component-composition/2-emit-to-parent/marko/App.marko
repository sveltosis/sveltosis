import AnswerButton from "./AnswerButton.marko";

class {
  onAnswerNo = function onAnswerNo() {
    this.state.canCome = false;
  };
  onAnswerYes = function onAnswerYes() {
    this.state.canCome = true;
  };

  onCreate() {
    this.state = { canCome: true };
  }
}

<p>Can I come ?</p>
<AnswerButton
  on-yes(event => component.onAnswerYes(event))
  on-no(event => component.onAnswerNo(event))
></AnswerButton>
<p style="font-size: 50px;">${state.canCome ? '😀' : '😥'}</p>